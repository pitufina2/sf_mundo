{% extends 'base.html.twig' %}

{% block body %}

	<h1>Detalle de presidentes</h1>

	<li>{{ presidente.nombre }} - {{ presidente.fechanac|date ('Y-m-d') }} -{{presidente.pais}}</li>

	<ul>

    	{% for presidente in presidente.pais %}

        	<li>{{ presidente.nombre }} - {{ presidente.continente }} - {{presidente.pais}} </li>

    	{% endfor %}

	</ul>

	<a href="{{ path('presidente') }}">Añadir presidente</a>

	{{ dump(presidente)}}

{% endblock %}
